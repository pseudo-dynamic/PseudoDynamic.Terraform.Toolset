<Project>

    <PropertyGroup>
        <LangVersion>preview</LangVersion>
        <Authors>Pseudo Dynamic authors and contributors</Authors>
        <Copyright>Copyright (c) 2022 Pseudo Dynamic authors and contributors</Copyright>
        <PackageLicenseFile Condition="'$(PackageLicenseFile)' == ''">LICENSE</PackageLicenseFile>
        <PackageIcon Condition="'$(PackageIcon)' == ''">icon.png</PackageIcon>
    </PropertyGroup>

    <ItemGroup>
        <None Include="$(MSBuildThisFileDirectory)\LICENSE">
            <Pack>True</Pack>
            <PackagePath></PackagePath>
        </None>

        <None Include="$(MSBuildThisFileDirectory)\res\icon.png">
            <Pack>True</Pack>
            <PackagePath></PackagePath>
        </None>
    </ItemGroup>

</Project>